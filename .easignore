# Ignore node modules
node_modules
npm-debug.log
yarn-error.log

# ✅ Keep lockfile for dependency consistency
# (DO NOT ignore package-lock.json or yarn.lock)

# Ignore build and cache folders
.expo
.expo-shared
dist
build
tmp
temp
.cache

# Ignore editor and system files
.vscode
.idea
.DS_Store
Thumbs.db

# Ignore backend and server-side code (optional)
server

# Optional: Ignore local environment files
.env
.env*.local

# Temporarily ignore assets with permission issues (optional)
# assets/images/*

# Ignore Git files (these are not needed in EAS build environment)
.git
.gitignore
